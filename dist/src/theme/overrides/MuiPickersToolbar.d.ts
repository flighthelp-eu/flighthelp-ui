import { Theme } from '@mui/material/styles';
declare const _default: (theme: Theme) => {
    styleOverrides: {
        root: {
            fontFamily: string;
        };
    };
};
export default _default;
